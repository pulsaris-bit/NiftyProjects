version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=your-secure-secret-here
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: always

volumes:
  data:
